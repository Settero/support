name: Run backend and emulator

on:
  workflow_dispatch:

jobs:
  run-system:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install backend dependencies
        run: pip install -r backend/requirements.txt

      - name: Install emulator dependencies
        run: pip install -r emulator/requirements.txt

      - name: Show backend files
        run: |
          pwd
          find backend -maxdepth 3 -type f

      - name: Start backend
        run: |
          python -u backend/app.py > backend.log 2>&1 &
          echo $! > backend.pid
          sleep 5
          echo "=== PS ==="
          ps -ef | grep python || true
          echo "=== PORTS ==="
          ss -ltnp || true
          echo "=== BACKEND LOG ==="
          cat backend.log || true

      - name: Wait for backend on localhost:5000
        run: |
          for i in {1..15}; do
            if curl -s http://localhost:5000/api/latest >/dev/null 2>&1; then
              echo "Backend is up"
              exit 0
            fi
            echo "Waiting for backend..."
            sleep 2
          done
          echo "Backend did not start in time"
          echo "=== BACKEND LOG ==="
          cat backend.log || true
          echo "=== PS ==="
          ps -ef | grep python || true
          echo "=== PORTS ==="
          ss -ltnp || true
          exit 1

      - name: Run emulator for 20 seconds
        run: |
          timeout 20s python emulator/emulator.py || true

      - name: Show backend log
        if: always()
        run: cat backend.log || true
